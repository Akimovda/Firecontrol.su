# Память проекта: firecontrol

Этот файл — рабочая память проекта. Сюда записываются: суть проекта, принятые решения,
договорённости, известные проблемы и ссылки на артефакты. Обновляется по ходу работы.

## Что это за проект

Инструменты для публикации статей на сайт WordPress (тематика — пожарная безопасность:
СОУЭ, сигнализация и т.п., судя по примерам в коде) через REST API, сразу с SEO-полями
(Yoast / Rank Math) и FAQ-блоком (видимый + JSON-LD Schema.org).

## Файлы

- `wp_publish.py` — CLI-скрипт публикации.
  - Вход: markdown-файл (`--file`) или stdin.
  - Конвертирует markdown → HTML, добавляет FAQ (видимый `<h2>/<h3>/<p>` блок + `FAQPage` JSON-LD).
  - Создаёт пост в WP через `wp-json/wp/v2/posts`, по умолчанию статус `draft`.
  - Умеет: категории/теги (создаёт при отсутствии), обложку (локальный файл или URL),
    SEO title/description/focus keyword под Yoast или Rank Math (автоопределение плагина
    через `wp-json/` namespaces, либо явно `--seo-plugin`).
  - Настройка через переменные окружения: `WP_URL`, `WP_USER`, `WP_APP_PASSWORD`
    (WordPress Application Password, не основной пароль).
- `wp-seo-rest-meta.php` — must-use плагин для WordPress (кладётся в `wp-content/mu-plugins/`,
  активация не нужна). Регистрирует защищённые SEO-мета-поля Yoast/Rank Math с
  `show_in_rest => true`, иначе REST API не может их записать. Доступ на запись — только
  пользователям с правом `edit_posts`.

## Важные зависимости между файлами

Без установленного на сайте `wp-seo-rest-meta.php` скрипт `wp_publish.py` всё равно создаст
пост и FAQ, но SEO-поля молча не запишутся (скрипт это детектит и предупреждает в выводе).

## GitHub Pages — статичное зеркало страниц сайта

В `docs/` лежит статичное HTML-зеркало (сделано `wget --page-requisites --convert-links`)
части страниц firecontrol.su — для GitHub Pages (репозиторий публичный, значит и
опубликованное зеркало видно всем). Источник: `/tmp/mirror_urls.txt` в момент создания
(2026-08-08) содержал 10 URL: главная, `/about_company/`, `/contact/`,
`/licenses-and-certificates/`, `/fire-audit/`, `/evacuation-plans/`,
`/fire-alarm-systems/` и 3 последние опубликованные статьи (id 901, 904, 908).
Наполняется постепенно, не всё сразу — по решению пользователя.

Чтобы добавить страницы позже: тот же `wget` на новый URL с `--directory-prefix=docs
--no-host-directories --convert-links --page-requisites --span-hosts --domains=firecontrol.su`.

Включить показ: в настройках GitHub-репозитория Settings → Pages → Source: Deploy from
branch → Branch: `main`, папка `/docs` → Save (я не могу включить это сам — нет токена
для GitHub API/`gh auth`, только push по SSH).

## Хостинг / SSH-доступ — важно не путать аккаунты

На Beget под одним и тем же провайдером существует НЕСКОЛЬКО SSH-аккаунтов с очень
похожими именами, но только один реально обслуживает живой firecontrol.su:

- **Правильный** (реальный докрут сайта, `87.236.16.25`): пользователь `akimovda_fc1`,
  путь `/home/a/akimovda/fire/public_html` (домен хостинг-аккаунта — "fire", НЕ
  "firecontrol.su"!). Тема `wp-content/themes/firecontrol/` с реальным `send.php`
  лежит именно тут. Доступ сохранён в `.env` (`SSH_*` переменные).
- **Неправильные/устаревшие** (старый, брошенный аккаунт на сервере `aramis.beget.ru`,
  тот же физический сервер что и правильный, но другой докрут-путь): пользователи
  `akimovda_fc`, `akimovda_new`, `akimovda_wpfc` — путь
  `/home/a/akimovda/firecontrol.su/public_html` (обратите внимание на домен в пути —
  тут он "firecontrol.su", а не "fire"). `send.php` там — версия от 2022 года,
  никак не связанная с реальным сайтом. Также туда же ведёт технический домен
  `akimovda.beget.tech` (это просто личный алиас того же сервера `aramis`, не новый сервер).
- Способ проверки, если снова возникнут сомнения: создать через SSH уникальный тестовый
  файл в `public_html` и тут же проверить `curl https://firecontrol.su/<файл>` — если
  файла нет на живом сайте (404), это не тот аккаунт.

## Известные проблемы

### ~~Форма обратной связи не долетает до Telegram-бота~~ — ИСПРАВЛЕНО 2026-08-08

Формы на страницах услуг (`class="forma"`) шлют `$.post` на
`wp-content/themes/firecontrol/send.php` (обычный PHP-файл темы на сервере, не WP REST).

**Причина (диагностировано 2026-08-07):** отправка в Telegram была сделана через
`fopen("https://api.telegram.org/bot{$token}/...")` без проверки результата — при неудаче
скрипт молча продолжал работу. `$return_arr` заполнялся только при ошибке, поэтому при
полном успехе (или при силентном фейле шага без собственной проверки) ответ был пустым
`json_encode([])` = `[]`, а фронтенд (`theme.js`, проверяет только `data.frm_check ==
'error'`) показывал "Ваше сообщение отправлено!" в любом случае — то есть форма могла
годами молча не доставлять сообщения, и никто бы не заметил.

**Фикс (2026-08-08):** получен верный SSH-доступ (см. раздел выше), сделан бэкап
(`send.php.bak-20260808` в той же папке на сервере), блок отправки в Telegram переписан
с `fopen()` на `curl` с проверкой HTTP-кода и `curl_error()` (залогирует через `error_log`
при неудаче), явно выставляется `frm_check = 'error'` только если ОБА получателя
(группа и личный чат) не получили сообщение. Проверено end-to-end реальной отправкой
через форму — сообщение подтверждённо дошло в Telegram.

### Формат телефона изменён + добавлена honeypot-защита от спама (2026-08-08)

По запросу пользователя маска телефона изменена с `+7 (999) 999 - 9999` на
`+7(999)999-99-99` — правки внесены СИНХРОННО в двух местах (иначе форма перестаёт
принимать любые номера, как в предыдущем баге):
- `theme.js` (строка с `$("input[name=phone-at]").mask(...)`) — JS-маска ввода.
- `send.php` (`$mask = "+7(XXX)XXX-XX-XX";`) — серверная валидация.

**Важно:** `theme.js` подключается с версией в query-string (`theme.js?v3`), которая
НЕ была захардкожена под автообновление WordPress — при правке файла обязательно
увеличивать версию (`?v3` → `?v4` и т.д. в `footer.php`), иначе у посетителей с закэшированным
JS форма будет присылать номер в старом формате, а сервер — его отклонять. Это уже один
раз произошло сразу после правки маски и было исправлено бампом версии.

Обнаружено, что антиспам в `ban.php` (чёрный список IP) полностью закомментирован и
фактически не работает. Вместо его реанимации добавлено **honeypot-поле**
(`name="website-at"`, визуально скрыто через `position:absolute;left:-9999px`,
`tabindex="-1"`, `aria-hidden="true"`) — вставлено в три места, где встречается форма
(`class="forma"`): `footer.php` (попап консультации, вызывается из шапки сайта),
`sidebar-service.php` (сайдбар "Разместить заказ" на всех страницах услуг),
`page-contact.php` (страница контактов). В `send.php` в начале обработки POST — если
`website-at` не пусто, скрипт молча возвращает `[]` (как при обычном успехе) и завершает
работу, ничего никуда не отправляя — бот не понимает, что его вычислили.
Проверено end-to-end: пустой honeypot — сообщение доходит; заполненный — не доходит,
при этом ответ сервера одинаковый в обоих случаях.

Бэкапы на сервере (в той же папке `wp-content/themes/firecontrol/`):
`send.php.bak-20260808`, `send.php.bak-20260808-mask`, `send.php.bak-20260808-honeypot-server`,
`theme.js.bak-20260808`, `footer.php.bak-20260808-honeypot`,
`sidebar-service.php.bak-20260808-honeypot`, `page-contact.php.bak-20260808-honeypot`.

**TODO:** эти изменения внесены напрямую на сервере через SSH и НЕ версионируются в этом
git-репозитории — стоит скачать актуальные `send.php`/`theme.js`/`footer.php`/
`sidebar-service.php`/`page-contact.php` в репозиторий (например в `theme/`) для трекинга
будущих изменений, по аналогии с `wp-seo-rest-meta.php`.

### `wp-seo-rest-meta.php` не был установлен на сервере + не работал для `page` (2026-08-08)

Обнаружено при попытке проставить SEO-поля странице `/evacuation-plans/` (id 105, `page`,
не `post`): запись через REST молча не проходила. Причины сразу две:
1. Папки `wp-content/mu-plugins/` на реальном сервере вообще не существовало — плагин из
   этого репозитория никогда не был туда залит (хотя `wp_publish.py` для ПОСТОВ и без него
   прекрасно работал — SEO-мета для `post` регистрирует сам Yoast из коробки).
2. Даже если бы был установлен, старая версия плагина регистрировала мета-поля только
   для `register_post_meta('post', ...)`, без `'page'` — а у Yoast свой REST-схема для
   `page` эти поля не выставляет вообще (проверено через `OPTIONS /wp-json/wp/v2/pages`).

**Исправлено:** `wp-seo-rest-meta.php` обновлён (регистрирует ключи для `post` И `page`
в цикле) и реально развёрнут в `wp-content/mu-plugins/` на сервере через SCP. Проверено:
`OPTIONS /wp-json/wp/v2/pages` теперь отдаёт все 6 ключей (Yoast+RankMath), запись
SEO-полей странице 105 подтверждена. Теперь SEO-правки через REST доступны для ЛЮБОЙ
из 25 страниц услуг, не только для постов.

### Секреты в открытом виде в коде темы

`send.php` содержит захардкоженные боевые секреты: токен Telegram-бота, токен мессенджера
Max, и полноценный долгоживущий Bearer-токен amoCRM (права `crm, files, files_delete...`,
годен примерно до 2030 года). Файл лежит в `wp-content/themes/firecontrol/` — при любой
утечке исходников (мисконфиг, бэкап) это полный доступ к CRM. Секреты уже были показаны
в чате с Claude при диагностике — рекомендована ротация всех трёх токенов и вынос их из
кода темы в переменные окружения. См. [[feedback-secrets-handling]] — то же правило,
которое действует и для наших собственных `.env`, должно применяться и на стороне сайта.

## Файлы (доп.)

- `wp_audit.py` — SEO-аудит опубликованных постов через REST API: тонкий контент,
  SEO title/desc, обложка+alt, alt у картинок в тексте, дубли H1/отсутствие H2.
  Сортирует по приоритету (скор). `--csv`/`--json` сохраняют полный отчёт.
- `wp_linkmap.py` — карта внутренних ссылок между постами и страницами: считает
  входящие/исходящие internal-ссылки, находит "сироты" (0 входящих). Отдаёт только
  сырые данные графа — смысловые рекомендации "откуда сослаться" делаются вручную/LLM
  по выгруженным заголовкам и выжимкам контента.

## Артефакты

- `reports/audit.csv`, `reports/audit.json` — полный аудит 31 опубликованного поста
  (2026-08-06). Топ-проблемные: id 280, 362, 298, 253, 291 (тонкий контент + нет SEO
  title + нет alt у обложки). У ВСЕХ 31 постов не заполнен alt у featured-картинки —
  системная проблема, а не единичный случай.
- `reports/linkmap.json` — карта внутренних ссылок (2026-08-06). 56 опубликованных
  элементов (31 пост + 25 страниц), 36 без единой входящей внутренней ссылки.
  Изолированные коммерческие страницы услуг (0 входящих и почти 0 исходящих):
  `/special-technical-conditions/` (СТУ), `/videonablyudenie/`, `/fire-alarm-systems/`,
  `/proektirovanie-slabotochnyh-sistem/`. Хороший пример кластера ссылок для
  подражания — `/evacuation-plans/` (7 входящих, 12 исходящих, статьи про планы
  эвакуации плотно связаны друг с другом и с услугой).

## Семантика по данным Wordstat — распределение фокусных ключей по страницам

Пользователь присылает частотности запросов (Яндекс Wordstat) по страницам услуг —
задача: не дать нескольким страницам/статьям конкурировать за один и тот же запрос
(каннибализация). Актуальное распределение (обновлять по мере поступления новых данных):

- **`/evacuation-plans/`** (page 105) — фокус «заказать план эвакуации при пожаре»
  (2026-08-08, было пусто — впервые заполнены title/desc/focus kw).
- **`/fire-audit/`** (page 100) — фокус «аудит пожарной безопасности» (топ-запрос 2192,
  уже был на нём, но title/desc были переразмерены — 102/161 симв. вместо 60/155 —
  сжаты 2026-08-08).
- **Пост 335 «Инспектор или независимая оценка»** — переключён на «независимая оценка
  пожарного риска» (493), раньше дублировал focus kw страницы `/fire-audit/`.
- **Пост 352 «Для чего нужен пожарный аудит?»** — переключён на информационный «чем
  является аудит пожарной безопасности» (304), тоже раньше дублировал `/fire-audit/`.
- **`/fire-safety-declaration/`** (page 157) — фокус kw был «декларация пожарной
  безопасности» (без «разработка»), переключён на «разработка декларации пожарной
  безопасности» (топ-1127, реальный кластер услуги). Каннибализации с другими
  материалами не было. Также добавлен новый раздел на странице — H2 «На какие здания
  нужна декларация пожарной безопасности» (кластер ~1800 запросов, ответа на него
  на странице не было) — вставлен перед существующим блоком про исключения.
- **`/categorization/`** (page 140) — фокус kw был «Категорирование помещений», а эта
  фраза вообще не встречается в реальных запросах — люди ищут «расчёт категории
  помещения» (топ-1, 3329) и варианты «по пожарной/взрывопожарной опасности».
  Переключено на «расчет категории помещения». Каннибализации с постом 295 нет —
  тот про ДРУГУЮ систему категорий (промышленная безопасность ОПО, классы I-IV по ФЗ),
  а не про категории помещений по пожарной/взрывопожарной опасности (СП 12.13130).
  Замеченный, но не реализованный кластер: «онлайн калькулятор/программа расчёта»
  (~660 запросов) — люди хотят бесплатный инструмент для самостоятельного расчёта;
  разработка калькулятора — отдельная задача, пропущено по решению пользователя.
- **`/fire-risk-calculation/`** (page 153) — была в целом в порядке (title 47, desc 146,
  фокус kw уже близкий к теме) — единственная правка: фокус kw переключён с
  «Расчет пожарного риска» (ед.ч.) на точную топ-фразу «расчет пожарных рисков»
  (мн.ч., 9387, топ-1 среди всех проанализированных кластеров). Каннибализации нет.
  Найден, но НЕ реализован контент-пробел: раздел «Нормативные документы» упоминает
  только приказы №382/404/644, а в Wordstat заметный суммарный объём (~1400) у номеров
  №1140/1084/533/272, которых на странице нет — не стал добавлять сам, т.к. не уверен,
  к каким именно объектам/случаям относится каждый документ (юридическая точность
  важна) — ждёт уточнения от пользователя.

Общий вывод: на сайте уже была скрытая каннибализация — несколько материалов на одну
и ту же общую тему получали почти одинаковый фокусный ключ вместо того, чтобы делить
кластер запросов между собой. При каждой новой пачке Wordstat-данных для очередной
страницы услуги — сначала проверять существующие статьи в том же смысловом кластере
(см. `reports/linkmap.json` и раздел про linkmap выше) и разводить ключи, а не дублировать.

## Открытые вопросы / TODO

(пусто — заполнять по ходу работы)

## Опубликованные статьи (черновики)

- ID 901 — «Нормы и правила противопожарной защиты при организации паркингов и станций
  технического ухода за транспортом» (2026-08-07), статус draft, категория «Пожарная
  безопасность», теги «паркинг»/«СТО». Исходник: `articles/parkingi-i-sto.md`.
  Редактировать: https://firecontrol.su/wp-admin/post.php?post=901&action=edit
- ID 904 — «Можно ли уйти от огнезащитной обработки несущих металлоконструкций путем
  разработки СТУ?» (2026-08-07), статус draft, категория «Пожарная безопасность», теги
  «СТУ»/«огнезащитная обработка». В тексте — первая входящая ссылка на изолированную
  страницу услуги `/special-technical-conditions/` (см. карту внутренних ссылок).
  536 слов — короче порога "тонкого контента" (800) из wp_audit.py, короткая юридическая
  статья по своей природе. Исходник: `articles/otkaz-ot-ognezaschity-stu.md`.
  Редактировать: https://firecontrol.su/wp-admin/post.php?post=904&action=edit
- ID 908 — «Утрачена исполнительная документация по АПС и СОУЭ: порядок восстановления
  и подводные камни» (2026-08-07), статус draft, категории «Пожарная безопасность» +
  «Пожарная сигнализация», теги «исполнительная документация»/«АПС»/«СОУЭ». Ссылки на
  `/fire-safety-document/` и `/fire-alarm-systems/`. Исходник:
  `articles/vosstanovlenie-ispolnitelnoy-dokumentacii.md`.
  Редактировать: https://firecontrol.su/wp-admin/post.php?post=908&action=edit

## Рабочий процесс для новых статей (сложился по ходу публикаций)

1. Из присланного текста убирать служебный мусор: домены-сноски источников
   (типа "grad.center" отдельной строкой), пометки темы сайта (дата обновления,
   "аудиоверсия" и т.п.) — не вставлять их в тело статьи как видимый текст.
2. Исправлять опечатки в присланном тексте при переносе в markdown (смысл не менять).
3. Заголовки секций — `##` (H2), никогда `#` (H1) — H1 страницы это заголовок поста.
4. Фото для обложки подбирать самостоятельно (Pexels/Unsplash, свободная лицензия),
   визуально проверять перед использованием (скачать и посмотреть через Read),
   указывать источник в `--image-caption` и осмысленный `--image-alt`.
5. Искать в тексте естественные места для внутренних ссылок на страницы услуг сайта,
   особенно на страницы-сироты без входящих ссылок (см. `reports/linkmap.json`
   и раздел про linkmap выше) — по одной-две ссылки на статью, не форсированно.
6. SEO title ≤60 символов, description ≤155, фокусный ключ по теме статьи.
7. Категории/теги — сначала проверять существующие через API, переиспользовать
   подходящие, создавать новые только когда действительно нет соответствия.
8. Публикация всегда как draft (по умолчанию у wp_publish.py) — не публиковать сразу.

## Правило: секреты

Секреты (WP_APP_PASSWORD, токены и т.п.) — только в `.env`, никогда не в память (memory)
и не в код. `.env` обязательно должен быть в `.gitignore`.
